# Route inventory

Source: `docs/reference-analysis.md` → section "Prompt 1 — Route inventory".

That file is the single authoritative capture, collected in-browser on 30 Aug 2026 via Claude in
Chrome (the cloud container cannot reach the reference site — see blocker B-1 in
`docs/OPEN-QUESTIONS.md`). It is not duplicated here; read it there so there is one source of truth.

- Route table, every URL actually loaded → `## Prompt 1 — Route inventory` → `### Route table`
- Dynamic URL patterns with real examples → `### URL patterns`
- Primary nav / mobile menu / footer → `### Primary navigation`, `### Mobile menu`, `### Footer`
- Pagination and query parameters → `### Pagination`, `### Query parameters`
- Unreachable routes and why → `### Inaccessible / not determined`

**Status column meaning here:** the reference site's HTTP status, not our build status. Build status
is tracked in `docs/PROGRESS.md`.
